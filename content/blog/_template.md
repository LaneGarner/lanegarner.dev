---
title: "Post title goes here"
date: "01-01-2026"
featuredImage: /blog/optional-image.jpg
---

Copy this file, rename it to your slug (e.g. `a11y-focus-management.md`),
update the frontmatter, and write. Delete the `featuredImage` line if the
post has no header image.

## A section heading

Body text in Markdown. Links, lists, `inline code`, and code blocks all
work:

```css
.example {
  color: var(--accent);
}
```

- Files starting with `_` (like this template) never appear on the site.
- See README.md in this folder for the full frontmatter reference.
